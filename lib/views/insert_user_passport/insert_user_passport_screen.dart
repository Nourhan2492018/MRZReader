import 'package:flutter/material.dart';

class InsertUserPassportScreen extends StatelessWidget {
  const InsertUserPassportScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Container(
        padding: const EdgeInsets.symmetric(horizontal: 10,),
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
